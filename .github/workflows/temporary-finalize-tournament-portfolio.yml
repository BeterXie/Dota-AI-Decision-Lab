name: Temporary finalize tournament portfolio

on:
  push:
    branches:
      - feat/ai-tournament-portfolio-quality-gate

permissions:
  contents: write

jobs:
  verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: dota_ai_decision_lab
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/dota_ai_decision_lab
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: feat/ai-tournament-portfolio-quality-gate
          fetch-depth: 1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          version: 0.12.0
          python-version: '3.14'
      - run: uv sync --frozen --extra dev
      - name: Apply final semantics
        run: |
          uv run python tools/temporary_prepare_finalize_helper.py
          uv run python tools/temporary_finalize_tournament_portfolio.py
      - name: Validate final semantics
        run: |
          uv run ruff check --fix app/ai app/evaluation tests/test_tournament_portfolio.py tests/test_tournament_quality.py tests/test_tournament_portfolio_unresolved_event.py
          uv run ruff format app/ai app/evaluation tests/test_tournament_portfolio.py tests/test_tournament_quality.py tests/test_tournament_portfolio_unresolved_event.py
          uv run ruff check app/ai app/evaluation tests/test_tournament_portfolio.py tests/test_tournament_quality.py tests/test_tournament_portfolio_unresolved_event.py
          uv run ruff check --select S app/ai app/evaluation
          uv run alembic upgrade head
          uv run alembic check
          uv run pytest -W error -q \
            tests/test_tournament_portfolio.py \
            tests/test_tournament_portfolio_late_result.py \
            tests/test_tournament_portfolio_postgres.py \
            tests/test_tournament_portfolio_unresolved_event.py \
            tests/test_tournament_quality.py \
            tests/test_tournament_global_leaderboard.py \
            tests/test_ai_coordinator.py \
            tests/test_ai_scheduling.py
      - name: Commit verified final semantics
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/ai app/evaluation tests/test_tournament_portfolio.py tests/test_tournament_quality.py
          git commit -m "Finalize tournament bankroll and quality sampling semantics"
          git push origin HEAD:feat/ai-tournament-portfolio-quality-gate
